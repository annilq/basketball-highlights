# 重构视频上传页面

## 1. 安装依赖

- 安装 `react-dropzone` 组件，用于实现拖动上传功能

## 2. 修改路由文件 `apps/app/routes/(app)/shot-detection.tsx`

### 2.1 调整UI布局

- 将视频上传组件放在上面，URL输入框放在下面

- 移除原有的开关切换，改为使用选项卡或直接显示两种上传方式

### 2.2 实现拖动上传功能

- 使用 `react-dropzone` 组件实现视频文件的拖动上传

- 支持点击选择文件和拖动文件两种方式

- 显示上传区域的视觉反馈（如拖入时的高亮效果）

- 显示已选择的文件信息

### 2.3 更新表单逻辑

- 保留原有的功能逻辑，确保两种上传方式都能正常工作

- 更新状态管理，支持文件上传和URL输入

- 确保API调用逻辑正确处理两种输入类型

## 3. 实现细节

### 3.1 组件结构

- 使用 `react-dropzone` 组件包裹上传区域

- 添加拖入状态的视觉反馈

- 显示文件选择按钮和提示文字

- 显示已选择的文件信息和预览

### 3.2 样式设计

- 保持与现有UI规范一致，使用Tailwind CSS样式

- 添加合适的间距和边框样式

- 实现拖入时的高亮效果

- 确保组件响应式设计

### 3.3 功能逻辑

- 支持多种视频格式

- 限制文件大小（可选）

- 提供清晰的用户反馈

- 与现有API调用逻辑集成

## 4. 测试策略

- 测试拖动上传功能正常工作

- 测试点击选择文件功能正常工作

- 测试URL输入方式仍然正常工作

- 测试表单提交逻辑正确处理两种输入类型

- 测试不同大小的视频文件

- 测试UI响应式设计

## 5. 注意事项

- 确保组件样式与现有UI规范一致

- 提供清晰的用户反馈和错误处理

- 确保性能良好，特别是处理大文件时

- 保持代码结构清晰，易于维护
