# 实现投篮检测可视化功能

## 需求分析

为`shot_detector.py`新增一个方法`visualize_shots`，用于根据视频生成可视化投篮检测结果图片帧，包含以下标记：

- 篮球检测框和红色轨迹
- 篮球架检测框和蓝色中心点
- 投篮检测状态（up/down区域）
- 投篮结果（make/miss标记）
- 显示关键信息：尝试次数、命中次数、投篮状态
- 图片保存路径为`visualize_shots`文件夹下与原视频同名

## 实现方案

### 1. 新增`visualize_shots`方法

在`ShotDetector`类中添加一个新方法，该方法将：

- 处理视频文件（类似`run`方法）
- 生成包含所有要求标记的可视化帧
- 将帧保存到指定目录

### 2. 关键实现细节

- 复用现有检测和可视化逻辑
- 添加保存帧到磁盘的功能
- 创建输出目录结构
- 确保所有标记元素正确显示

### 3. 实现步骤

#### 步骤1：添加方法框架

```python
def visualize_shots(self, output_dir="visualize_shots"):
    # 实现视频处理和帧保存逻辑
    pass
```

#### 步骤2：实现视频处理循环

- 读取视频帧
- 执行目标检测
- 清理位置数据
- 执行投篮检测
- 生成可视化帧

#### 步骤3：添加可视化标记

确保包含所有要求的标记：

- 篮球检测框和红色轨迹（已有）
- 篮球架检测框和蓝色中心点（已有）
- 投篮状态（up/down区域）
- 投篮结果（make/miss标记，已有）
- 关键信息显示（已有）

#### 步骤4：添加目录创建和帧保存

- 创建输出目录
- 生成帧文件名
- 保存帧到磁盘

### 4. 代码修改点

#### 文件：`/Users/yunqi/Documents/develop/basketball-highlights/apps/shot-detector/shot_detector.py`

- 添加`visualize_shots`方法
- 可能需要添加额外的导入（如`os`）

### 5. 预期效果

- 运行`visualize_shots`方法后，会在`visualize_shots`目录下生成一系列图片帧
- 每个帧包含完整的投篮检测可视化信息
- 用户可以直观查看模型识别结果

## 技术要点

- 复用现有检测和可视化逻辑
- 添加文件IO操作
- 确保性能和内存使用合理
- 保持代码结构清晰
- 合理利用现有逻辑，避免冗余
- 生成测试用例

这个实现将帮助用户直观地查看模型的识别结果，找出为什么没有检测到命中投篮的问题。
